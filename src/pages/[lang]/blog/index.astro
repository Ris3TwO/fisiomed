---
import { LOCALES } from "@/i18n/config";

export function getStaticPaths() {
  return LOCALES.map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params;

return Astro.redirect(`/${lang}/blog/1`, 301);
---
